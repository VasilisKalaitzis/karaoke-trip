import { FETCH_SONG_LYRICS } from "./types";

// fetch invoice by its ID
// request: server invoice
// parameters:
//        invoiceId intereger
export const fetchSongLyrics = (aritst, song) => (dispatch, getState) => {

};
